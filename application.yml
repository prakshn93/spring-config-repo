spring:
  application:
    name: user-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test
    hikari:
      username: test
      password: test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
com:
  saa:
    firstName: "prakash"
    lastName: "nagesh"

server:
  port: 8082
